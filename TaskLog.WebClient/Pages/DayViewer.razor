@if (ProjectTasks != null)
{
    @foreach (var task in ProjectTasks)
    {
        <TaskView ProjectTask="@task" />
    }
}

@code {
    [Parameter]
    public List<ProjectTask> ProjectTasks { get; set; }
}
